#ifndef __MAIN_H
#define __MAIN_H

/*
 * Includes
 */
#include <string.h>
#include "filelist.h"
#include "display.h"
#include "event.h"


/*
 * Macros
 */
#define min(a, b)	((a) < (b) ? (a) : (b))


#endif
